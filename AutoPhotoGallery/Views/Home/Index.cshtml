@model PhotoListModel
@{
    ViewData["Title"] = "Home Page";
}

<div>
    @{
        foreach (string name in Model.PhotoList)
        {
            <div>
                @name
            </div>
        }
    }

</div>
